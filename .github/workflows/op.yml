name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]  
  workflow_dispatch:

jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{  }} 
          user: yinchunyuan
          repo: 666
          template: repository 
          plugin_commits: yes  
          plugin_commits_limit: 100  
